# =============================================================================
# TrulyIAS Multi-Agent Scheduling Assistant — Requirements
# =============================================================================
# Python: 3.10 – 3.12 (recommended, tested, stable)
# Architecture: LangGraph 0.2+ Multi-Agent + Groq LLM + SQLite + Streamlit
# Deployment: Streamlit Community Cloud (python-3.12 via runtime.txt)
# =============================================================================

# ── LangChain & LangGraph Ecosystem ───────────────────────────────────────────
langchain>=0.3.0,<0.4.0          # Core orchestration & LCEL chains
langchain-core>=0.3.0,<0.4.0     # Base abstractions, tool binding
langchain-groq>=0.1.0            # ChatGroq LLM wrapper (Groq API)
langgraph>=0.2.0                 # Multi-agent state-machine workflow engine
langgraph-checkpoint-sqlite>=2.0.0  # SqliteSaver for native LangGraph persistence

# ── Web Interface ───────────────────────────────────────────────────────────
streamlit>=1.38.0                # Session-state chat UI, sidebar, caching

# ── Configuration & Secrets ───────────────────────────────────────────────────
python-dotenv>=1.0.0             # Load GROQ_API_KEY / WEBHOOK_URL from .env
pydantic>=2.0.0                  # Type-safe data validation for state models
pydantic-settings>=2.0.0         # Map env vars to Pydantic models

# ── LLM API Client ────────────────────────────────────────────────────────────
groq>=0.9.0                      # Groq SDK — openai/gpt-oss-120b @ 500 tok/s free tier

# ── HTTP & Webhook Notifications ────────────────────────────────────────────
requests>=2.31.0                 # POST to Webhook.site / Pipedream for mock notifications

# ── Date/Time Handling ────────────────────────────────────────────────────────
python-dateutil>=2.8.0           # Relative date parsing: "tomorrow" → YYYY-MM-DD

# ── SQLite & Persistence ──────────────────────────────────────────────────────
# (built-in sqlite3 — no extra package needed)
